#include<stdio.h>
int table_mull(int n){
    for(int i = 0; i <= 10; ++i){
        printf("%d * %d = %d\n",n,i,n * i);
    }
    return 0;
}
int main(){
    int n = 0;
    scanf("%d",&n);
    table_mull(n);
    return 0;
}
