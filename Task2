#include<stdio.h>
int table_mull(int n){
    for(int i = 1; i <= n; ++i){
        for(int j = 1; j <= n; ++j){
            printf("%d * %d = %d\n",i,j,i * j);
        }
    }
    return 0;
}
int main(){
    int n = 10;
    table_mull(n);
    return 0;
}
